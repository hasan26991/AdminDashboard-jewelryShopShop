// import { ThemeProvider } from '@mui/material'

// import { useDateTimePicker, DateTimePickerProps } from './useDateTimePicker'

// const DateTimePicker = <T,>(props: DateTimePickerProps<T>) => {
//   const { FinalComponent, datePickerTheme } = useDateTimePicker(props)

//   return <ThemeProvider theme={datePickerTheme}>{FinalComponent}</ThemeProvider>
// }

// export default DateTimePicker
